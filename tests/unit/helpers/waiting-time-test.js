import { waitingTime } from 'help-queue/helpers/waiting-time';
import { module, test } from 'qunit';

module('Unit | Helper | waiting time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = waitingTime([42]);
  assert.ok(result);
});
