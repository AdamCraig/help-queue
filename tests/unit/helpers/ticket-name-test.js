import { ticketName } from 'help-queue/helpers/ticket-name';
import { module, test } from 'qunit';

module('Unit | Helper | ticket name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ticketName([42]);
  assert.ok(result);
});
